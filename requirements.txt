# OntoloHub Dependencies
# 安装：`pip install -r requirements.txt`
# 可选：-r requirements-dev.txt  开发依赖
#
# 这份清单覆盖根 monorepo 的所有 Python 包（apps/api + apps/cli + packages/core）。
# 真正的元数据见各子包 pyproject.toml；本文件只是 pip 的便捷入口。

# ===== Core API =====
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# ===== Database =====
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
aiosqlite>=0.20.0

# ===== Validation =====
pyshacl>=0.26.0

# ===== Config & utils =====
python-dotenv>=1.0.0
pyyaml>=6.0.0
python-multipart>=0.0.9
httpx>=0.27.0
rich>=13.7.0
typer>=0.12.0

# ===== Connector framework secrets (HIA-71) =====
cryptography>=42.0.0  # Fernet 对称加密，用于连接器 config.secret_fields

# ===== Cache / Session store (HIA-64) =====
# M1 起引入：候选/映射缓存、SHACL 报告缓存、限流与会话。
# 不强制启动 — 没 Redis 时降级为「无缓存」模式（HIA-64 退出条件）。
redis[hiredis]>=5.0.0